interface:
  display_name: "Frontend Design"
  short_description: "DeKoi UI concept and build lens"
  default_prompt: "Use $frontend-design to design and build this DeKoi UI surface, then use Impeccable for polish if needed."
