interface:
  display_name: "Bunny Style Review"
  short_description: "Local branch reviews with failure-path lenses."
  default_prompt: "Use $bunny-style-review, or say bunny, to review this local branch, PR, or diff for bugs, regressions, edge cases, and actionable nitpicks."
