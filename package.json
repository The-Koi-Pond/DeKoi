{
  "name": "dekoi",
  "private": true,
  "version": "0.0.0",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:tauri-frontend": "vite --host 127.0.0.1 --port 1420 --strictPort",
    "runtime:fixture": "node scripts/remote-runtime-fixture.mjs",
    "check:desktop-contracts": "node scripts/check-desktop-contracts.mjs",
    "check:storage-contracts": "node scripts/check-storage-contracts.mjs",
    "check:provider-secret-safety": "node scripts/check-provider-secret-safety.mjs",
    "check:runtime-contracts": "node scripts/check-runtime-contracts.mjs",
    "check:frontend-boundaries": "node scripts/check-frontend-boundaries.mjs",
    "check:bunny-review": "node .github/bunny-review/check-bunny-review.mjs && node .github/bunny-review/run_guidance_check.mjs --self-test && node .github/bunny-review/run_guidance_check.mjs",
    "check:rust": "cargo check --manifest-path src-tauri/Cargo.toml",
    "check": "pnpm check:desktop-contracts && pnpm check:storage-contracts && pnpm check:provider-secret-safety && pnpm check:runtime-contracts && pnpm check:frontend-boundaries && pnpm check:bunny-review && pnpm test && pnpm build && pnpm lint && pnpm check:rust",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "pnpm build && playwright test",
    "test:ui:run": "playwright test",
    "preview": "vite preview",
    "tauri": "node scripts/run-tauri.mjs",
    "tauri:dev": "node scripts/run-tauri.mjs dev",
    "tauri:build": "node scripts/run-tauri.mjs build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.60.0",
    "@tauri-apps/cli": "^2.11.4",
    "@types/node": "^24.12.3",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^10.3.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.6.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.59.2",
    "vite": "^8.0.12",
    "vitest": "^4.1.9"
  }
}
